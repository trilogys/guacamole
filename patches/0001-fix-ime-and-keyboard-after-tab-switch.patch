diff --git a/guacamole/src/main/frontend/src/app/client/controllers/clientController.js b/guacamole/src/main/frontend/src/app/client/controllers/clientController.js
--- a/guacamole/src/main/frontend/src/app/client/controllers/clientController.js
+++ b/guacamole/src/main/frontend/src/app/client/controllers/clientController.js
@@ -34,6 +34,7 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
 
     // Required services
     const $location              = $injector.get('$location');
+    const $timeout               = $injector.get('$timeout');
     const authenticationService  = $injector.get('authenticationService');
     const connectionGroupService = $injector.get('connectionGroupService');
     const clipboardService       = $injector.get('clipboardService');
@@ -94,6 +95,20 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
      */
     var DEL_KEY = 0xFFFF;
 
+    /**
+     * Stable time after which the automatic page refresh attempt count resets.
+     *
+     * @type Number
+     */
+    var AUTO_REFRESH_RESET_DELAY = 60000;
+
+    /**
+     * Pending automatic page refresh and stable-period reset timers.
+     */
+    var autoRefreshTimer = null;
+    var autoRefreshResetTimer = null;
+    var automaticRefreshPending = false;
+
     /**
      * Menu-specific properties.
      */
@@ -106,6 +121,14 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
          */
         shown : false,
 
+        /**
+         * Whether confirmed connection failures should trigger a delayed
+         * refresh of the entire current browser page.
+         *
+         * @type Boolean
+         */
+        automaticRefresh : preferenceService.preferences.automaticRefresh === true,
+
         /**
          * The currently selected input method. This may be any of the values
          * defined within preferenceService.inputMethods.
@@ -144,6 +167,21 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
         $scope.menu.shown = false;
     };
 
+    /**
+     * Requests immediate recovery of Guacamole keyboard and IME capture.
+     */
+    $scope.recoverKeyboardInput = function recoverKeyboardInput() {
+        $scope.$emit('guacForceInputRecovery');
+        $scope.closeMenu();
+    };
+
+    /**
+     * Toggles the Guacamole menu when requested by the global keyboard fallback.
+     */
+    $scope.$on('guacToggleMenuRequested', function toggleMenuRequested() {
+        $scope.menu.shown = !$scope.menu.shown;
+    });
+
     /**
      * Applies any changes to connection parameters made by the user within the
      * Guacamole menu to the given ManagedClient. If no client is supplied,
@@ -454,6 +492,13 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
 
     });
 
+    // Persist automatic page refresh immediately. The recovery state watcher
+    // below cancels any pending refresh as soon as this is disabled.
+    $scope.$watch('menu.automaticRefresh', function setAutomaticRefresh(enabled) {
+        preferenceService.preferences.automaticRefresh = enabled === true;
+        preferenceService.save();
+    });
+
     // Update client state/behavior as visibility of the Guacamole menu changes
     $scope.$watch('menu.shown', function menuVisibilityChanged(menuShown, menuShownPreviousState) {
 
@@ -661,6 +706,181 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
         return _.findIndex($scope.clientGroup.clients, client => client.clientState.tunnelUnstable) !== -1;
     };
 
+    /**
+     * Returns whether the given client still has an active connection.
+     */
+    var isClientConnectionActive = function isClientConnectionActive(client) {
+        const state = client.clientState.connectionState;
+        return state === ManagedClientState.ConnectionState.WAITING
+            || state === ManagedClientState.ConnectionState.CONNECTED;
+    };
+
+    /**
+     * Returns whether the given client still represents a page state for
+     * which confirmed network or remote-control failure recovery is useful.
+     */
+    var isClientConnectionRecoverable = function isClientConnectionRecoverable(client) {
+        const state = client.clientState.connectionState;
+        return isClientConnectionActive(client)
+            || state === ManagedClientState.ConnectionState.DISCONNECTED
+            || state === ManagedClientState.ConnectionState.TUNNEL_ERROR;
+    };
+
+    /**
+     * Cancels any pending automatic page refresh.
+     */
+    var cancelAutoRefresh = function cancelAutoRefresh() {
+        if (autoRefreshTimer) {
+            $timeout.cancel(autoRefreshTimer);
+            autoRefreshTimer = null;
+        }
+        automaticRefreshPending = false;
+    };
+
+    /**
+     * Cancels any pending reset of the page refresh loop guard.
+     */
+    var cancelAutoRefreshReset = function cancelAutoRefreshReset() {
+        if (autoRefreshResetTimer) {
+            $timeout.cancel(autoRefreshResetTimer);
+            autoRefreshResetTimer = null;
+        }
+    };
+
+    /**
+     * Returns whether any current connection has an active file transfer.
+     */
+    var hasActiveTransfers = function hasActiveTransfers() {
+        return $scope.clientGroup && _.findIndex($scope.clientGroup.clients,
+            client => ManagedClient.hasActiveTransfers(client)) !== -1;
+    };
+
+    /**
+     * Returns whether reconnecting one or more clients should be offered after
+     * a confirmed period of tunnel instability.
+     *
+     * @returns {Boolean}
+     *     true if at least one client may benefit from being reconnected.
+     */
+    $scope.isConnectionRecoveryAvailable = function isConnectionRecoveryAvailable() {
+        return $scope.clientGroup && _.findIndex($scope.clientGroup.clients, client =>
+            client.reconnectSuggested && isClientConnectionRecoverable(client)) !== -1;
+    };
+
+    /**
+     * Returns whether an automatic full-page refresh is currently pending.
+     */
+    $scope.isAutomaticRefreshPending = function isAutomaticRefreshPending() {
+        return automaticRefreshPending;
+    };
+
+    /**
+     * Returns whether the current page can be refreshed automatically now.
+     */
+    var canAutoRefresh = function canAutoRefresh() {
+        return $scope.menu.automaticRefresh
+            && $scope.isConnectionRecoveryAvailable()
+            && !hasActiveTransfers()
+            && preferenceService.canAutomaticallyRefresh();
+    };
+
+    /**
+     * Queues one bounded refresh of the entire current browser page.
+     */
+    var queueAutoRefresh = function queueAutoRefresh() {
+        if (autoRefreshTimer || !canAutoRefresh())
+            return;
+
+        automaticRefreshPending = true;
+        autoRefreshTimer = $timeout(function autoRefreshPage() {
+            autoRefreshTimer = null;
+            automaticRefreshPending = false;
+            if (canAutoRefresh())
+                preferenceService.refreshPage(false);
+        }, preferenceService.getAutomaticRefreshDelay());
+    };
+
+    /**
+     * Refreshes the entire current browser page at the user's request.
+     */
+    $scope.refreshPage = function refreshPage() {
+        cancelAutoRefresh();
+        cancelAutoRefreshReset();
+        preferenceService.refreshPage(true);
+    };
+
+    /**
+     * Dismisses the recovery suggestion without reconnecting.
+     */
+    $scope.dismissConnectionRecovery = function dismissConnectionRecovery() {
+        $scope.clientGroup.clients.forEach(client => {
+            client.controlUnresponsive = false;
+            client.reconnectSuggested = false;
+        });
+        cancelAutoRefresh();
+    };
+
+    /**
+     * Returns whether all current clients have remained active and stable.
+     */
+    var areAllConnectionsStable = function areAllConnectionsStable() {
+        return $scope.clientGroup && $scope.clientGroup.clients.length
+            && _.findIndex($scope.clientGroup.clients, client =>
+                !isClientConnectionActive(client)
+                || client.clientState.tunnelUnstable
+                || client.reconnectSuggested) === -1;
+    };
+
+    /**
+     * Clears the cross-refresh loop guard after one stable minute.
+     */
+    var scheduleAutoRefreshReset = function scheduleAutoRefreshReset() {
+        if (autoRefreshResetTimer)
+            return;
+
+        autoRefreshResetTimer = $timeout(function resetAutoRefreshAttempts() {
+            autoRefreshResetTimer = null;
+            if (areAllConnectionsStable())
+                preferenceService.resetAutomaticRefresh();
+        }, AUTO_REFRESH_RESET_DELAY);
+    };
+
+    // Queue/cancel automatic page refresh as preference, tunnel, and transfer
+    // state changes. Manual recovery remains available independently.
+    $scope.$watchCollection(function getAutoRefreshState() {
+        if (!$scope.clientGroup)
+            return [];
+
+        const state = $scope.clientGroup.clients.map(client => [
+            client.id,
+            client.reconnectSuggested,
+            client.clientState.tunnelUnstable,
+            client.clientState.connectionState,
+            ManagedClient.hasActiveTransfers(client)
+        ].join(':'));
+
+        state.push('automaticRefresh:' + $scope.menu.automaticRefresh);
+        return state;
+    }, function autoRefreshStateChanged() {
+
+        if (!$scope.clientGroup) {
+            cancelAutoRefresh();
+            cancelAutoRefreshReset();
+            return;
+        }
+
+        if (canAutoRefresh())
+            queueAutoRefresh();
+        else
+            cancelAutoRefresh();
+
+        if (areAllConnectionsStable())
+            scheduleAutoRefreshReset();
+        else
+            cancelAutoRefreshReset();
+
+    });
+
     /**
      * Immediately disconnects all currently-focused clients, if any.
      */
@@ -668,9 +888,15 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
 
         // Disconnect if client is available
         if ($scope.clientGroup) {
+            cancelAutoRefresh();
+            cancelAutoRefreshReset();
+            preferenceService.resetAutomaticRefresh();
             $scope.clientGroup.clients.forEach(client => {
-                if (client.clientProperties.focused)
+                if (client.clientProperties.focused) {
+                    client.controlUnresponsive = false;
+                    client.reconnectSuggested = false;
                     client.client.disconnect();
+                }
             });
         }
 
@@ -870,6 +1096,10 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
 
     // Clean up when view destroyed
     $scope.$on('$destroy', function clientViewDestroyed() {
+
+        cancelAutoRefresh();
+        cancelAutoRefreshReset();
+
         setAttachedGroup(null);
 
         // always unset fullscreen mode to not confuse user 
diff --git a/guacamole/src/main/frontend/src/app/client/directives/guacClient.js b/guacamole/src/main/frontend/src/app/client/directives/guacClient.js
--- a/guacamole/src/main/frontend/src/app/client/directives/guacClient.js
+++ b/guacamole/src/main/frontend/src/app/client/directives/guacClient.js
@@ -100,6 +100,97 @@ angular.module('client').directive('guacClient', [function guacClient() {
          */
         const main = $element[0];
 
+        // Downstream modification: coalesce high-frequency mouse movement so
+        // clicks cannot become trapped behind stale movement during weak
+        // network conditions.
+
+        /**
+         * Minimum time between transmitted mouse movement states, in
+         * milliseconds. Button transitions are never delayed.
+         *
+         * @type Number
+         */
+        const MOUSE_MOVE_INTERVAL = 33;
+
+        /**
+         * Most recent mouse movement waiting to be sent, if any.
+         *
+         * @type Object|null
+         */
+        let pendingMouseMove = null;
+
+        /**
+         * Timeout ID of a queued mouse movement, or null if none is queued.
+         *
+         * @type Number|null
+         */
+        let pendingMouseMoveTimer = null;
+
+        /**
+         * Creates an independent copy of the given mouse state. Guacamole's
+         * mouse event objects may be reused after the event handler returns.
+         */
+        const copyMouseState = function copyMouseState(state) {
+            return {
+                x      : state.x,
+                y      : state.y,
+                left   : state.left,
+                middle : state.middle,
+                right  : state.right,
+                up     : state.up,
+                down   : state.down
+            };
+        };
+
+        /**
+         * Immediately sends the latest queued mouse movement, if any.
+         */
+        const flushPendingMouseMove = function flushPendingMouseMove() {
+            if (pendingMouseMoveTimer !== null) {
+                $window.clearTimeout(pendingMouseMoveTimer);
+                pendingMouseMoveTimer = null;
+            }
+
+            const state = pendingMouseMove;
+            pendingMouseMove = null;
+            if (state && client)
+                client.sendMouseState(state, true);
+        };
+
+        /**
+         * Cancels any queued mouse movement without sending it.
+         */
+        const cancelPendingMouseMove = function cancelPendingMouseMove() {
+            if (pendingMouseMoveTimer !== null)
+                $window.clearTimeout(pendingMouseMoveTimer);
+            pendingMouseMoveTimer = null;
+            pendingMouseMove = null;
+        };
+
+        /**
+         * Coalesces mouse movement while forwarding all button transitions
+         * immediately and in the correct position.
+         */
+        const sendMouseEventState = function sendMouseEventState(event) {
+            if (event.type !== 'mousemove') {
+                flushPendingMouseMove();
+                client.sendMouseState(event.state, true);
+
+                // A press is an intentional control action. If repeated
+                // presses receive no subsequent remote display sync, the
+                // ManagedClient will request recovery of the wedged session.
+                if (event.type === 'mousedown' && $scope.client.noteControlInput)
+                    $scope.client.noteControlInput();
+                return;
+            }
+
+            pendingMouseMove = copyMouseState(event.state);
+            if (pendingMouseMoveTimer === null) {
+                pendingMouseMoveTimer = $window.setTimeout(
+                    flushPendingMouseMove, MOUSE_MOVE_INTERVAL);
+            }
+        };
+
         /**
          * Guacamole mouse event object, wrapped around the main client
          * display.
@@ -241,7 +332,7 @@ angular.module('client').directive('guacClient', [function guacClient() {
 
             // Send mouse state, show cursor if necessary
             display.showCursor(!localCursor);
-            client.sendMouseState(event.state, true);
+            sendMouseEventState(event);
 
             // Broadcast the mouse event
             $rootScope.$broadcast(getMouseEventName(event), event, client);
@@ -272,7 +363,7 @@ angular.module('client').directive('guacClient', [function guacClient() {
 
             // Send mouse state, ensure cursor is visible
             scrollToMouse(event.state);
-            client.sendMouseState(event.state, true);
+            sendMouseEventState(event);
 
             // Broadcast the mouse event
             $rootScope.$broadcast(getMouseEventName(event), event, client);
@@ -328,6 +419,9 @@ angular.module('client').directive('guacClient', [function guacClient() {
         // Attach any given managed client
         $scope.$watch('client', function attachManagedClient(managedClient) {
 
+            // Never forward a movement queued for the previous connection.
+            cancelPendingMouseMove();
+
             // Remove any existing display
             displayContainer.innerHTML = "";
 
@@ -562,6 +656,9 @@ angular.module('client').directive('guacClient', [function guacClient() {
         // Forward all mouse events
         mouse.onEach(['mousedown', 'mousemove', 'mouseup'], handleMouseEvent);
 
+        // Prevent queued movement from surviving directive teardown.
+        $scope.$on('$destroy', cancelPendingMouseMove);
+
         // Hide software cursor when mouse leaves display
         mouse.on('mouseout', function() {
             if (!display) return;
diff --git a/guacamole/src/main/frontend/src/app/client/directives/guacClientNotification.js b/guacamole/src/main/frontend/src/app/client/directives/guacClientNotification.js
--- a/guacamole/src/main/frontend/src/app/client/directives/guacClientNotification.js
+++ b/guacamole/src/main/frontend/src/app/client/directives/guacClientNotification.js
@@ -52,8 +52,8 @@ angular.module('client').directive('guacClientNotification', [function guacClien
         // Required services
         const $location              = $injector.get('$location');
         const authenticationService  = $injector.get('authenticationService');
-        const guacClientManager      = $injector.get('guacClientManager');
         const guacTranslate          = $injector.get('guacTranslate');
+        const preferenceService      = $injector.get('preferenceService');
         const requestService         = $injector.get('requestService');
         const userPageService        = $injector.get('userPageService');
 
@@ -67,10 +67,10 @@ angular.module('client').directive('guacClientNotification', [function guacClien
         $scope.status = false;
 
         /**
-         * All error codes for which automatic reconnection is appropriate when a
-         * client error occurs.
+         * All error codes for which automatic page refresh is appropriate when
+         * a client error occurs.
          */
-        const CLIENT_AUTO_RECONNECT = {
+        const CLIENT_AUTO_REFRESH = {
             0x0200: true,
             0x0202: true,
             0x0203: true,
@@ -81,10 +81,10 @@ angular.module('client').directive('guacClientNotification', [function guacClien
         };
 
         /**
-         * All error codes for which automatic reconnection is appropriate when a
-         * tunnel error occurs.
+         * All error codes for which automatic page refresh is appropriate when
+         * a tunnel error occurs.
          */
-        const TUNNEL_AUTO_RECONNECT = {
+        const TUNNEL_AUTO_REFRESH = {
             0x0200: true,
             0x0202: true,
             0x0203: true,
@@ -129,24 +129,25 @@ angular.module('client').directive('guacClientNotification', [function guacClien
         }, requestService.WARN);
 
         /**
-         * Action which replaces the current client with a newly-connected client.
+         * Action which refreshes the entire current browser page.
          */
-        const RECONNECT_ACTION = {
-            name      : "CLIENT.ACTION_RECONNECT",
+        const REFRESH_PAGE_ACTION = {
+            name      : "CLIENT.ACTION_REFRESH_PAGE",
             className : "reconnect button",
-            callback  : function reconnectCallback() {
-                $scope.client = guacClientManager.replaceManagedClient($scope.client.id);
-                $scope.status = false;
+            callback  : function refreshPageCallback() {
+                preferenceService.refreshPage(true);
             }
         };
 
         /**
-         * The reconnect countdown to display if an error or status warrants an
-         * automatic, timed reconnect.
+         * The refresh countdown to display if an error or status warrants an
+         * automatic, timed full-page refresh.
          */
-        const RECONNECT_COUNTDOWN = {
-            text: "CLIENT.TEXT_RECONNECT_COUNTDOWN",
-            callback: RECONNECT_ACTION.callback,
+        const REFRESH_PAGE_COUNTDOWN = {
+            text: "CLIENT.TEXT_REFRESH_PAGE_COUNTDOWN",
+            callback: function automaticRefreshPage() {
+                preferenceService.refreshPage(false);
+            },
             remaining: 15
         };
 
@@ -194,9 +195,9 @@ angular.module('client').directive('guacClientNotification', [function guacClien
             // Build array of available actions
             let actions;
             if (NAVIGATE_HOME_ACTION)
-                actions = [ NAVIGATE_HOME_ACTION, RECONNECT_ACTION, LOGOUT_ACTION ];
+                actions = [ NAVIGATE_HOME_ACTION, REFRESH_PAGE_ACTION, LOGOUT_ACTION ];
             else
-                actions = [ RECONNECT_ACTION, LOGOUT_ACTION ];
+                actions = [ REFRESH_PAGE_ACTION, LOGOUT_ACTION ];
 
             // Get any associated status code
             const status = $scope.client.clientState.statusCode;
@@ -209,7 +210,8 @@ angular.module('client').directive('guacClientNotification', [function guacClien
                     title: "CLIENT.DIALOG_HEADER_CONNECTING",
                     text: {
                         key : "CLIENT.TEXT_CLIENT_STATUS_" + connectionState.toUpperCase()
-                    }
+                    },
+                    actions : [ REFRESH_PAGE_ACTION ]
                 };
             }
 
@@ -222,7 +224,11 @@ angular.module('client').directive('guacClientNotification', [function guacClien
                 const defaultErrorId = errorPrefix + "DEFAULT";
 
                 // Determine whether the reconnect countdown applies
-                const countdown = (status in CLIENT_AUTO_RECONNECT) ? RECONNECT_COUNTDOWN : null;
+                // Downstream modification: automatic full-page refresh is
+                // opt-in and bounded across reloads. The manual action remains.
+                const countdown = preferenceService.preferences.automaticRefresh
+                    && preferenceService.canAutomaticallyRefresh()
+                    && (status in CLIENT_AUTO_REFRESH) ? REFRESH_PAGE_COUNTDOWN : null;
 
                 // Use the guacTranslate service to determine if there is a translation for
                 // this error code; if not, use the default
@@ -251,7 +257,9 @@ angular.module('client').directive('guacClientNotification', [function guacClien
                 const defaultErrorId = errorPrefix + "DEFAULT";
 
                 // Determine whether the reconnect countdown applies
-                const countdown = (status in TUNNEL_AUTO_RECONNECT) ? RECONNECT_COUNTDOWN : null;
+                const countdown = preferenceService.preferences.automaticRefresh
+                    && preferenceService.canAutomaticallyRefresh()
+                    && (status in TUNNEL_AUTO_REFRESH) ? REFRESH_PAGE_COUNTDOWN : null;
 
                 // Use the guacTranslate service to determine if there is a translation for
                 // this error code; if not, use the default
diff --git a/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js b/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js
--- a/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js
+++ b/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js
@@ -162,6 +162,22 @@ angular.module('client').directive('guacTiledClients', [function guacTiledClient
             };
         };
 
+        // Downstream modification: restore client keyboard focus after tab changes.
+
+        /**
+         * Ensures at least one client in the currently attached group can receive
+         * keyboard events after browser focus or visibility changes.
+         */
+        var restoreClientKeyboardFocus = function restoreClientKeyboardFocus() {
+            if ($scope.clientGroup)
+                ManagedClientGroup.verifyFocus($scope.clientGroup);
+        };
+
+        // Text-input mode and raw-keyboard mode use different browser input
+        // paths, but both require a focused ManagedClient before sending keys.
+        $scope.$on('guacTextInputFocusRequested', restoreClientKeyboardFocus);
+        $scope.$on('guacKeyboardFocusRequested', restoreClientKeyboardFocus);
+
         /**
          * @borrows ManagedClientGroup.hasMultipleClients
          */
diff --git a/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css b/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css
--- a/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css
+++ b/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css
@@ -54,3 +54,15 @@
     background-repeat: no-repeat;
 
 }
+
+#connection-warning .actions {
+    display: flex;
+    flex-wrap: wrap;
+    justify-content: flex-end;
+    gap: 0.5em;
+    margin-top: 0.5em;
+}
+
+#connection-warning .actions button {
+    font-size: 1em;
+}
diff --git a/guacamole/src/main/frontend/src/app/client/templates/client.html b/guacamole/src/main/frontend/src/app/client/templates/client.html
--- a/guacamole/src/main/frontend/src/app/client/templates/client.html
+++ b/guacamole/src/main/frontend/src/app/client/templates/client.html
@@ -41,8 +41,32 @@
     </div>
 
     <!-- Connection stability warning -->
-    <div id="connection-warning" ng-show="isConnectionUnstable()">
-        {{'CLIENT.TEXT_CLIENT_STATUS_UNSTABLE' | translate}}
+    <div id="connection-warning"
+         ng-show="isConnectionUnstable() || isConnectionRecoveryAvailable()">
+        <div class="message" ng-if="isConnectionUnstable()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_UNSTABLE' | translate}}
+        </div>
+        <div class="message"
+             ng-if="isConnectionUnstable() && isAutomaticRefreshPending()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_RECOVERED_REFRESHING' | translate}}
+        </div>
+        <div class="message"
+             ng-if="!isConnectionUnstable() && isAutomaticRefreshPending()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_RECOVERED_REFRESHING' | translate}}
+        </div>
+        <div class="message"
+             ng-if="!isConnectionUnstable() && !isAutomaticRefreshPending()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_RECOVERED_SLOW' | translate}}
+        </div>
+        <div class="actions">
+            <button class="reconnect" ng-click="refreshPage()">
+                {{'CLIENT.ACTION_REFRESH_PAGE' | translate}}
+            </button>
+            <button ng-if="!isConnectionUnstable() || isAutomaticRefreshPending()"
+                    ng-click="dismissConnectionRecovery()">
+                {{'CLIENT.ACTION_DISMISS_RECOVERY' | translate}}
+            </button>
+        </div>
     </div>
     
     <!-- Menu -->
@@ -128,11 +152,40 @@
                                model-only="true"></guac-form>
                 </div>
 
+                <!-- Connection recovery -->
+                <div class="menu-section" id="connection-recovery-settings">
+                    <h3>{{'CLIENT.SECTION_HEADER_CONNECTION_RECOVERY' | translate}}</h3>
+                    <div class="content">
+                        <div class="choice">
+                            <label>
+                                <input id="automatic-refresh" type="checkbox"
+                                       ng-model="menu.automaticRefresh">
+                                {{'CLIENT.NAME_AUTOMATIC_REFRESH' | translate}}
+                            </label>
+                            <p class="caption">
+                                <label for="automatic-refresh">{{'CLIENT.HELP_AUTOMATIC_REFRESH' | translate}}</label>
+                            </p>
+                        </div>
+                        <div class="choice">
+                            <button class="button" ng-click="refreshPage()">
+                                {{'CLIENT.ACTION_REFRESH_PAGE' | translate}}
+                            </button>
+                            <p class="caption">{{'CLIENT.HELP_REFRESH_PAGE_NOW' | translate}}</p>
+                        </div>
+                    </div>
+                </div>
+
                 <!-- Input method -->
                 <div class="menu-section" id="keyboard-settings">
                     <h3>{{'CLIENT.SECTION_HEADER_INPUT_METHOD' | translate}}</h3>
                     <div class="content">
 
+                        <!-- Manual keyboard/IME recovery -->
+                        <div class="choice">
+                            <button class="button" ng-click="recoverKeyboardInput()">{{'CLIENT.ACTION_RECOVER_KEYBOARD' | translate}}</button>
+                            <p class="caption">{{'CLIENT.HELP_RECOVER_KEYBOARD' | translate}}</p>
+                        </div>
+
                         <!-- No IME -->
                         <div class="choice">
                             <label><input id="ime-none" name="input-method" ng-change="closeMenu()" ng-model="menu.inputMethod" type="radio" value="none"> {{'CLIENT.NAME_INPUT_METHOD_NONE' | translate}}</label>
diff --git a/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js b/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js
--- a/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js
+++ b/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js
@@ -35,6 +35,7 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
     const ManagedDisplay         = $injector.get('ManagedDisplay');
     const ManagedFilesystem      = $injector.get('ManagedFilesystem');
     const ManagedFileUpload      = $injector.get('ManagedFileUpload');
+    const ManagedFileTransferState = $injector.get('ManagedFileTransferState');
     const ManagedShareLink       = $injector.get('ManagedShareLink');
 
     // Required services
@@ -55,12 +56,37 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
     const guacVideo               = $injector.get('guacVideo');
 
     /**
-     * The minimum amount of time to wait between updates to the client
-     * thumbnail, in milliseconds.
+     * Additional time that tunnel instability must persist before the user is
+     * warned, in milliseconds. The underlying tunnel timeout remains
+     * unchanged and continues to detect actual connection failure.
      *
      * @type Number
      */
-    var THUMBNAIL_UPDATE_FREQUENCY = 5000;
+    var TUNNEL_UNSTABLE_WARNING_DELAY = 3000;
+
+    /**
+     * Maximum time to wait for a remote display sync after repeated control
+     * input before treating the remote control path as unresponsive.
+     *
+     * @type Number
+     */
+    var CONTROL_RESPONSE_TIMEOUT = 8000;
+
+    /**
+     * Minimum number of mouse presses required before the control response
+     * watchdog may request recovery.
+     *
+     * @type Number
+     */
+    var CONTROL_RESPONSE_MIN_INPUTS = 3;
+
+    /**
+     * Maximum acceptable display queue or browser rendering delay when a
+     * remote sync is used as evidence that control remains responsive.
+     *
+     * @type Number
+     */
+    var CONTROL_MAX_DISPLAY_DELAY = 3000;
 
     /**
      * Object which serves as a surrogate interface, encapsulating a Guacamole
@@ -221,6 +247,22 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
          */
         this.clientState = template.clientState || new ManagedClientState();
 
+        /**
+         * Whether a confirmed period of tunnel instability has occurred and
+         * reconnecting this client should be offered if control remains slow.
+         *
+         * @type Boolean
+         */
+        this.reconnectSuggested = template.reconnectSuggested || false;
+
+        /**
+         * Whether repeated control input has received no subsequent remote
+         * display synchronization while the tunnel itself remains open.
+         *
+         * @type Boolean
+         */
+        this.controlUnresponsive = template.controlUnresponsive || false;
+
         /**
          * Properties associated with the display and behavior of the Guacamole
          * client.
@@ -390,9 +432,193 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
             tunnel : tunnel
         });
 
+        // Downstream modification: avoid showing transient instability caused
+        // by browser background throttling or short network stalls.
+        var unstableWarningTimer = null;
+
+        // ChainedTunnel forwards the active child's state without updating its
+        // own state property, so retain the latest forwarded value explicitly.
+        var currentTunnelState = Guacamole.Tunnel.State.CLOSED;
+
+        // The tunnel ping only proves that the browser can reach the web
+        // application. Track remote display syncs separately so a wedged
+        // guacd/protocol connection can be recovered without a page refresh.
+        var controlResponseTimer = null;
+        var controlInputsSinceSync = 0;
+        var serverSyncGeneration = 0;
+        var minimumSyncOffset = null;
+        var displayProcessingLag = 0;
+
+        // Keep Guacamole's built-in display statistics disabled unless the
+        // user has intentionally clicked. Gathering them for every rendered
+        // frame adds avoidable work during large remote-browser repaints.
+        var display = client.getDisplay();
+        display.statisticWindow = 0;
+        display.onstatistics = function displayStatisticsAvailable(stats) {
+            if (stats.processingLag !== null)
+                displayProcessingLag = stats.processingLag;
+        };
+
+        /**
+         * Enables a small statistics window only while control responsiveness
+         * is actively being checked.
+         */
+        var enableControlResponseStatistics = function enableControlResponseStatistics() {
+            displayProcessingLag = 0;
+            display.statisticWindow = 1000;
+        };
+
+        /**
+         * Stops gathering per-frame display statistics.
+         */
+        var disableControlResponseStatistics = function disableControlResponseStatistics() {
+            display.statisticWindow = 0;
+            displayProcessingLag = 0;
+        };
+
+        /**
+         * Cancels the pending control-response check and forgets accumulated
+         * input which has since been acknowledged by remote display activity.
+         */
+        var resetControlResponseWatchdog = function resetControlResponseWatchdog() {
+            if (controlResponseTimer !== null)
+                $window.clearTimeout(controlResponseTimer);
+            controlResponseTimer = null;
+            controlInputsSinceSync = 0;
+            disableControlResponseStatistics();
+        };
+
+        /**
+         * Stops foreground responsiveness sampling as soon as the page is
+         * hidden. A fresh user gesture will start a new check after return.
+         */
+        var controlVisibilityChanged = function controlVisibilityChanged() {
+            if ($document[0].hidden)
+                resetControlResponseWatchdog();
+        };
+
+        /**
+         * Records an intentional mouse press and verifies that some remote
+         * display synchronization follows. Multiple presses are required to
+         * avoid reconnecting merely because the user clicked an inert area.
+         */
+        managedClient.noteControlInput = function noteControlInput() {
+
+            if ($document[0].hidden
+                    || currentTunnelState !== Guacamole.Tunnel.State.OPEN)
+                return;
+
+            if (!controlInputsSinceSync)
+                enableControlResponseStatistics();
+            controlInputsSinceSync++;
+            if (controlResponseTimer !== null)
+                return;
+
+            var initialSyncGeneration = serverSyncGeneration;
+            controlResponseTimer = $window.setTimeout(function controlResponseTimedOut() {
+                controlResponseTimer = null;
+
+                if (controlInputsSinceSync < CONTROL_RESPONSE_MIN_INPUTS
+                        || serverSyncGeneration !== initialSyncGeneration
+                        || currentTunnelState !== Guacamole.Tunnel.State.OPEN
+                        || $document[0].hidden) {
+                    resetControlResponseWatchdog();
+                    return;
+                }
+
+                resetControlResponseWatchdog();
+                $rootScope.$evalAsync(function markControlUnresponsive() {
+                    if (currentTunnelState === Guacamole.Tunnel.State.OPEN
+                            && !$document[0].hidden
+                            && !managedClient.reconnectSuggested) {
+                        managedClient.controlUnresponsive = true;
+                        managedClient.reconnectSuggested = true;
+                    }
+                });
+
+            }, CONTROL_RESPONSE_TIMEOUT);
+
+        };
+
+        /**
+         * Cancels any pending warning that the tunnel appears unstable.
+         */
+        var cancelUnstableWarningTimer = function cancelUnstableWarningTimer() {
+            if (unstableWarningTimer !== null) {
+                $window.clearTimeout(unstableWarningTimer);
+                unstableWarningTimer = null;
+            }
+        };
+
+        /**
+         * Shows the tunnel instability warning only if the tunnel remains
+         * unstable and the page remains visible for the full grace period.
+         */
+        var scheduleUnstableWarning = function scheduleUnstableWarning() {
+
+            cancelUnstableWarningTimer();
+            ManagedClientState.setTunnelUnstable(managedClient.clientState, false);
+
+            // A background page cannot usefully display this warning. If the
+            // tunnel is still unstable on return, visibilitychange will start
+            // a fresh grace period.
+            if ($document[0].hidden)
+                return;
+
+            unstableWarningTimer = $window.setTimeout(function showUnstableWarning() {
+                unstableWarningTimer = null;
+
+                // Tunnel state and page visibility may have changed while the
+                // timer was pending. Recheck both within Angular's digest.
+                $rootScope.$evalAsync(function updateUnstableWarning() {
+                    if (currentTunnelState === Guacamole.Tunnel.State.UNSTABLE
+                            && !$document[0].hidden) {
+                        ManagedClientState.setTunnelUnstable(
+                            managedClient.clientState, true);
+                        managedClient.controlUnresponsive = false;
+                        managedClient.reconnectSuggested = true;
+                    }
+                });
+
+            }, TUNNEL_UNSTABLE_WARNING_DELAY);
+
+        };
+
+        /**
+         * Restarts the warning grace period when an unstable connection
+         * returns to a visible page, and suppresses the warning while hidden.
+         */
+        var tunnelVisibilityChanged = function tunnelVisibilityChanged() {
+            if (currentTunnelState !== Guacamole.Tunnel.State.UNSTABLE)
+                return;
+
+            $rootScope.$evalAsync(function updateUnstableWarningForVisibility() {
+                scheduleUnstableWarning();
+            });
+        };
+
+        /**
+         * Begins monitoring a persistently unstable tunnel.
+         */
+        var beginUnstableWarning = function beginUnstableWarning() {
+            resetControlResponseWatchdog();
+            $document[0].addEventListener('visibilitychange', tunnelVisibilityChanged, false);
+            scheduleUnstableWarning();
+        };
+
+        /**
+         * Clears all warning state once the tunnel changes state.
+         */
+        var endUnstableWarning = function endUnstableWarning() {
+            cancelUnstableWarningTimer();
+            $document[0].removeEventListener('visibilitychange', tunnelVisibilityChanged, false);
+            ManagedClientState.setTunnelUnstable(managedClient.clientState, false);
+        };
+
         // Fire events for tunnel errors
         tunnel.onerror = function tunnelError(status) {
             $rootScope.$apply(function handleTunnelError() {
+                endUnstableWarning();
                 ManagedClientState.setConnectionState(managedClient.clientState,
                     ManagedClientState.ConnectionState.TUNNEL_ERROR,
                     status.code);
@@ -410,28 +636,36 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
 
         // Update connection state as tunnel state changes
         tunnel.onstatechange = function tunnelStateChanged(state) {
+            currentTunnelState = state;
             $rootScope.$evalAsync(function updateTunnelState() {
                 
                 switch (state) {
 
                     // Connection is being established
                     case Guacamole.Tunnel.State.CONNECTING:
+                        endUnstableWarning();
+                        $document[0].addEventListener(
+                            'visibilitychange', controlVisibilityChanged, false);
                         ManagedClientState.setConnectionState(managedClient.clientState,
                             ManagedClientState.ConnectionState.CONNECTING);
                         break;
 
                     // Connection is established / no longer unstable
                     case Guacamole.Tunnel.State.OPEN:
-                        ManagedClientState.setTunnelUnstable(managedClient.clientState, false);
+                        endUnstableWarning();
                         break;
 
                     // Connection is established but misbehaving
                     case Guacamole.Tunnel.State.UNSTABLE:
-                        ManagedClientState.setTunnelUnstable(managedClient.clientState, true);
+                        beginUnstableWarning();
                         break;
 
                     // Connection has closed
                     case Guacamole.Tunnel.State.CLOSED:
+                        endUnstableWarning();
+                        resetControlResponseWatchdog();
+                        $document[0].removeEventListener(
+                            'visibilitychange', controlVisibilityChanged, false);
                         ManagedClientState.setConnectionState(managedClient.clientState,
                             ManagedClientState.ConnectionState.DISCONNECTED);
                         break;
@@ -538,17 +772,36 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
             });
         };
 
-        // Automatically update the client thumbnail
-        client.onsync = function syncReceived() {
-
-            var thumbnail = managedClient.thumbnail;
-            var timestamp = new Date().getTime();
-
-            // Update thumbnail if it doesn't exist or is old
-            if (!thumbnail || timestamp - thumbnail.timestamp >= THUMBNAIL_UPDATE_FREQUENCY) {
-                $rootScope.$apply(function updateClientThumbnail() {
-                    ManagedClient.updateThumbnail(managedClient);
-                });
+        // Track remote display progress without generating a synchronous
+        // full-display thumbnail from this latency-sensitive sync callback.
+        // Initial and final thumbnails are still captured by client state
+        // transitions above.
+        client.onsync = function syncReceived(timestamp) {
+
+            var localTimestamp = new Date().getTime();
+
+            // Guacamole sync timestamps are arbitrary but monotonic. The
+            // smallest observed local/remote offset is therefore the best
+            // available baseline; growth beyond that baseline is queued
+            // display latency rather than simple clock offset.
+            var syncOffset = localTimestamp - timestamp;
+            if (minimumSyncOffset === null || syncOffset < minimumSyncOffset)
+                minimumSyncOffset = syncOffset;
+            var displayQueueDelay = Math.max(0, syncOffset - minimumSyncOffset);
+
+            // Only timely syncs demonstrate that the latest controls can be
+            // observed. Old frames arriving from an overloaded remote browser
+            // must not indefinitely mask a growing display/input backlog.
+            if (displayQueueDelay <= CONTROL_MAX_DISPLAY_DELAY
+                    && displayProcessingLag <= CONTROL_MAX_DISPLAY_DELAY) {
+                serverSyncGeneration++;
+                resetControlResponseWatchdog();
+                if (managedClient.controlUnresponsive) {
+                    $rootScope.$evalAsync(function clearControlUnresponsive() {
+                        managedClient.controlUnresponsive = false;
+                        managedClient.reconnectSuggested = false;
+                    });
+                }
             }
 
         };
@@ -958,6 +1211,24 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
         return !!(client && client.uploads && client.uploads.length);
     };
 
+    /**
+     * Returns whether the given client has a file transfer which has not yet
+     * completed or failed.
+     *
+     * @param {GuacamoleClient} client
+     *     The client for which active file transfers should be checked.
+     *
+     * @returns {boolean}
+     *     true if at least one file transfer is pending or open.
+     */
+    ManagedClient.hasActiveTransfers = function hasActiveTransfers(client) {
+        return !!(client && client.uploads && _.find(client.uploads, upload => {
+            const state = upload.transferState.streamState;
+            return state === ManagedFileTransferState.StreamState.IDLE
+                || state === ManagedFileTransferState.StreamState.OPEN;
+        }));
+    };
+
     /**
      * Store the thumbnail of the given managed client within the connection
      * history under its associated ID. If the client is not connected, this
diff --git a/guacamole/src/main/frontend/src/app/index/controllers/indexController.js b/guacamole/src/main/frontend/src/app/index/controllers/indexController.js
--- a/guacamole/src/main/frontend/src/app/index/controllers/indexController.js
+++ b/guacamole/src/main/frontend/src/app/index/controllers/indexController.js
@@ -177,11 +177,17 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
 
     // Add default destination for input events
     var sink = new Guacamole.InputSink();
-    $document[0].body.appendChild(sink.getElement());
+    var sinkElement = sink.getElement();
+
+    // Downstream modification: raw-keyboard mode must bypass local IMEs so
+    // the remote desktop's current input language remains authoritative.
+    sinkElement.readOnly = true;
+    sinkElement.setAttribute('inputmode', 'none');
+    $document[0].body.appendChild(sinkElement);
 
     // Create event listeners at the global level
     var keyboard = new Guacamole.Keyboard($document[0]);
-    keyboard.listenTo(sink.getElement());
+    keyboard.listenTo(sinkElement);
 
     // Broadcast keydown events
     keyboard.onkeydown = function onkeydown(keysym) {
@@ -221,6 +227,8 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
 
     // Release all keys when window loses focus
     $window.onblur = function () {
+        keyboardRestorePendingUserGesture = true;
+        keyboardMenuShortcutActive = false;
         keyboard.reset();
     };
 
@@ -250,6 +258,342 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
 
     };
 
+    // Downstream modification: restore global keyboard input after tab changes.
+
+    /**
+     * Keysyms of modifier keys which may remain pressed on the remote desktop
+     * if the browser backgrounds the page before delivering their keyup events.
+     *
+     * @type {!number[]}
+     */
+    var REMOTE_MODIFIER_KEYSYMS = [
+        0xFE03, /* AltGr / ISO level 3 shift */
+        0xFFE1, /* Left shift */
+        0xFFE2, /* Right shift */
+        0xFFE3, /* Left ctrl */
+        0xFFE4, /* Right ctrl */
+        0xFFE7, /* Left meta */
+        0xFFE8, /* Right meta */
+        0xFFE9, /* Left alt */
+        0xFFEA, /* Right alt */
+        0xFFEB, /* Left super / Windows */
+        0xFFEC, /* Right super / Windows */
+        0xFFED, /* Left hyper */
+        0xFFEE  /* Right hyper */
+    ];
+
+    /**
+     * Timeout ID of a queued keyboard restoration, or null if none is queued.
+     * focus and visibilitychange often fire together and should be coalesced.
+     *
+     * @type {?number}
+     */
+    var keyboardRestoreTimer = null;
+
+    /**
+     * Whether a background/freeze event requires one final synchronous restore
+     * from the next trusted user gesture.
+     *
+     * @type {boolean}
+     */
+    var keyboardRestorePendingUserGesture = false;
+
+    /**
+     * Whether the native Guacamole menu shortcut is already being handled.
+     * This prevents repeated keydown events from reopening the menu.
+     *
+     * @type {boolean}
+     */
+    var keyboardMenuShortcutActive = false;
+
+    /**
+     * Cancels any queued asynchronous keyboard restoration.
+     */
+    var cancelKeyboardRestoreTimer = function cancelKeyboardRestoreTimer() {
+        if (keyboardRestoreTimer !== null) {
+            $window.clearTimeout(keyboardRestoreTimer);
+            keyboardRestoreTimer = null;
+        }
+    };
+
+    /**
+     * Returns whether the given element is within a visible local form field.
+     * In that case the hidden Guacamole input sink must not steal focus.
+     *
+     * @param {Element} element
+     *     The element to test.
+     *
+     * @returns {boolean}
+     */
+    var isVisibleLocalInput = function isVisibleLocalInput(element) {
+
+        var textInputTarget = $document[0].querySelector('.text-input .target');
+        while (element && element !== $document[0].body) {
+
+            if (element === sink.getElement() || element === textInputTarget)
+                return false;
+
+            var tagName = element.tagName ? element.tagName.toLowerCase() : '';
+            var isLocalInput = tagName === 'input'
+                    || tagName === 'textarea'
+                    || tagName === 'select'
+                    || tagName === 'button'
+                    || tagName === 'a'
+                    || element.isContentEditable;
+
+            if (isLocalInput) {
+                var rect = element.getBoundingClientRect();
+                return rect.width > 0 && rect.height > 0
+                        && rect.right > 0 && rect.bottom > 0;
+            }
+
+            element = element.parentNode;
+
+        }
+
+        return false;
+
+    };
+
+    /**
+     * Returns whether focus intentionally belongs to a visible local form field.
+     *
+     * @returns {boolean}
+     */
+    var hasVisibleLocalInputFocus = function hasVisibleLocalInputFocus() {
+        return isVisibleLocalInput($document[0].activeElement);
+    };
+
+    /**
+     * Returns whether the given event target belongs to the remote desktop
+     * input surface, including either Guacamole input target.
+     *
+     * @param {Element} element
+     *     The element to test.
+     *
+     * @returns {boolean}
+     */
+    var isRemoteKeyboardTarget = function isRemoteKeyboardTarget(element) {
+
+        var textInputTarget = $document[0].querySelector('.text-input .target');
+        if (element === sink.getElement() || element === textInputTarget
+                || element === $document[0].body
+                || element === $document[0].documentElement)
+            return true;
+
+        return !!(element && element.closest && element.closest('.client-body'));
+
+    };
+
+    /**
+     * Immediately restores keyboard forwarding. Synchronous restoration is used
+     * only for the first trusted pointer gesture after a background/freeze.
+     *
+     * @param {boolean} userInitiated
+     *     Whether this invocation is running within a trusted user gesture.
+     *
+     * @param {boolean} ignoreExistingLocalFocus
+     *     Whether focus held by a previous local form control should be ignored.
+     *
+     * @returns {boolean}
+     *     true if restoration was performed, false otherwise.
+     */
+    var restoreRemoteKeyboardInputNow = function restoreRemoteKeyboardInputNow(
+            userInitiated, ignoreExistingLocalFocus) {
+
+        if ($document[0].hidden || !$document[0].hasFocus()
+                || $scope.applicationState !== ApplicationState.READY
+                || !hasActiveTunnel()
+                || (!ignoreExistingLocalFocus && hasVisibleLocalInputFocus()))
+            return false;
+
+        $scope.$broadcast('guacKeyboardFocusRequested');
+
+        keyboard.reset();
+        angular.forEach(REMOTE_MODIFIER_KEYSYMS, function releaseModifier(keysym) {
+            $scope.$broadcast('guacSyntheticKeyup', keysym);
+        });
+
+        var inputFocusEvent = $scope.$broadcast(
+                'guacInputFocusRestoreRequested', userInitiated);
+        if (!inputFocusEvent.defaultPrevented) {
+            var sinkElement = sink.getElement();
+            sinkElement.value = '';
+            sinkElement.blur();
+            if (userInitiated)
+                sinkElement.focus();
+            else
+                sink.focus();
+        }
+
+        return true;
+
+    };
+
+    /**
+     * Restores raw keyboard forwarding after returning to the browser tab. This
+     * is the path used when Guacamole input mode is "None" and Microsoft Pinyin
+     * or another IME runs inside the remote Windows session.
+     */
+    var restoreRemoteKeyboardInput = function restoreRemoteKeyboardInput() {
+
+        if ($document[0].hidden || !$document[0].hasFocus()
+                || $scope.applicationState !== ApplicationState.READY
+                || !hasActiveTunnel() || hasVisibleLocalInputFocus()
+                || keyboardRestoreTimer !== null)
+            return;
+
+        keyboardRestoreTimer = $window.setTimeout(function deferKeyboardRestore() {
+
+            keyboardRestoreTimer = null;
+
+            if ($document[0].hidden || !$document[0].hasFocus()
+                    || $scope.applicationState !== ApplicationState.READY
+                    || !hasActiveTunnel() || hasVisibleLocalInputFocus())
+                return;
+
+            $scope.$evalAsync(function restoreKeyboardWithinDigest() {
+                restoreRemoteKeyboardInputNow(false, false);
+            });
+
+        }, 0);
+
+    };
+
+    // Some browsers do not reliably fire window blur for tab switches. Always
+    // reset tracked keys on visibility loss and restore the input sink on return.
+    var keyboardVisibilityChanged = function keyboardVisibilityChanged() {
+
+        if ($document[0].hidden) {
+            keyboardRestorePendingUserGesture = true;
+            keyboardMenuShortcutActive = false;
+            cancelKeyboardRestoreTimer();
+            keyboard.reset();
+        }
+        else
+            restoreRemoteKeyboardInput();
+
+    };
+
+    /**
+     * Marks keyboard input as needing a trusted-gesture restore after the page
+     * lifecycle freezes or resumes.
+     */
+    var keyboardPageLifecycleChanged = function keyboardPageLifecycleChanged(e) {
+
+        keyboardRestorePendingUserGesture = true;
+        keyboardMenuShortcutActive = false;
+
+        if (e.type === 'freeze' || e.type === 'pagehide') {
+            cancelKeyboardRestoreTimer();
+            keyboard.reset();
+        }
+        else
+            restoreRemoteKeyboardInput();
+
+    };
+
+    /**
+     * Performs the final restoration synchronously on a trusted mouse, touch,
+     * or pointer gesture over the remote desktop after the page has returned
+     * from the background. Multiple event types are intentionally supported
+     * because the earliest event may run before the browser has restored focus.
+     */
+    var keyboardUserGesture = function keyboardUserGesture(e) {
+
+        if (!keyboardRestorePendingUserGesture || $document[0].hidden
+                || e.isTrusted === false || !isRemoteKeyboardTarget(e.target)
+                || isVisibleLocalInput(e.target))
+            return;
+
+        cancelKeyboardRestoreTimer();
+
+        // pointerdown/mousedown/touchstart run before the browser's default
+        // focus action, which may immediately steal focus again. Keep recovery
+        // armed until the bubbling click performs the final restoration.
+        if (restoreRemoteKeyboardInputNow(true, true) && e.type === 'click')
+            keyboardRestorePendingUserGesture = false;
+
+    };
+
+    /**
+     * Restores keyboard capture before Guacamole handles the first key after a
+     * background event. Ctrl-Alt-K always forces recovery, while the native
+     * Ctrl-Alt-Shift shortcut also recovers input before directly opening the
+     * Guacamole menu. Both shortcuts work even if lifecycle events were omitted.
+     */
+    var keyboardKeyGesture = function keyboardKeyGesture(e) {
+
+        var altGraph = e.getModifierState
+                && e.getModifierState('AltGraph');
+        var forceRecovery = e.ctrlKey && e.altKey && !e.shiftKey
+                && !altGraph
+                && (e.code === 'KeyK' || e.key === 'K' || e.key === 'k');
+        var forceMenu = e.ctrlKey && e.altKey && e.shiftKey && !altGraph;
+
+        if (forceRecovery || forceMenu) {
+            e.preventDefault();
+            e.stopImmediatePropagation();
+
+            if (e.repeat || (forceMenu && keyboardMenuShortcutActive))
+                return;
+
+            keyboardMenuShortcutActive = forceMenu;
+            keyboardRestorePendingUserGesture = true;
+        }
+
+        if (!keyboardRestorePendingUserGesture || $document[0].hidden
+                || e.isTrusted === false
+                || (!forceRecovery && !forceMenu
+                        && (!isRemoteKeyboardTarget(e.target)
+                        || isVisibleLocalInput(e.target))))
+            return;
+
+        cancelKeyboardRestoreTimer();
+
+        if (restoreRemoteKeyboardInputNow(true, true))
+            keyboardRestorePendingUserGesture = false;
+
+        if (forceMenu)
+            $scope.$evalAsync(function toggleGuacamoleMenu() {
+                $scope.$broadcast('guacToggleMenuRequested');
+            });
+
+    };
+
+    /**
+     * Allows the native Guacamole menu shortcut to be used again after any of
+     * its modifier keys has been released.
+     */
+    var keyboardKeyReleased = function keyboardKeyReleased(e) {
+        if (!(e.ctrlKey && e.altKey && e.shiftKey))
+            keyboardMenuShortcutActive = false;
+    };
+
+    /**
+     * Forces full keyboard/input recovery from a Guacamole menu action.
+     */
+    $scope.$on('guacForceInputRecovery', function forceInputRecovery() {
+        keyboardRestorePendingUserGesture = true;
+        cancelKeyboardRestoreTimer();
+        if (restoreRemoteKeyboardInputNow(true, true))
+            keyboardRestorePendingUserGesture = false;
+    });
+
+    $document[0].addEventListener('visibilitychange', keyboardVisibilityChanged, true);
+    $document[0].addEventListener('freeze', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('resume', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('fullscreenchange', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('pointerlockchange', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('pointerdown', keyboardUserGesture, true);
+    $document[0].addEventListener('mousedown', keyboardUserGesture, true);
+    $document[0].addEventListener('touchstart', keyboardUserGesture, true);
+    $document[0].addEventListener('click', keyboardUserGesture, false);
+    $window.addEventListener('keydown', keyboardKeyGesture, true);
+    $window.addEventListener('keyup', keyboardKeyReleased, true);
+    $window.addEventListener('pagehide', keyboardPageLifecycleChanged, true);
+    $window.addEventListener('pageshow', keyboardPageLifecycleChanged, true);
+
     // If we're logged in and not connected to anything, periodically check
     // whether the current session is still valid. If the session has expired,
     // refresh the auth state to reshow the login screen (rather than wait for
@@ -278,9 +622,12 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
     $window.addEventListener('cut',   clipboardService.resyncClipboard);
     $window.addEventListener('focus', function focusGained(e) {
 
-        // Only recheck clipboard if it's the window itself that gained focus
-        if (e.target === $window)
-            clipboardService.resyncClipboard();
+        // Only act if it is the window itself that gained focus.
+        if (e.target !== $window)
+            return;
+
+        clipboardService.resyncClipboard();
+        restoreRemoteKeyboardInput();
 
     }, true);
 
diff --git a/guacamole/src/main/frontend/src/app/settings/services/preferenceService.js b/guacamole/src/main/frontend/src/app/settings/services/preferenceService.js
--- a/guacamole/src/main/frontend/src/app/settings/services/preferenceService.js
+++ b/guacamole/src/main/frontend/src/app/settings/services/preferenceService.js
@@ -119,6 +119,17 @@ angular.module('settings').provider('preferenceService', ['$injector',
      */
     this.preferences = {
 
+        // Downstream modification: full-page automatic refresh is opt-in so
+        // users can choose between delayed and manual recovery.
+
+        /**
+         * Whether confirmed connection failures should trigger a delayed,
+         * bounded automatic refresh of the current browser page.
+         *
+         * @type Boolean
+         */
+        automaticRefresh : false,
+
         /**
          * Whether translation of touch to mouse events should emulate an
          * absolute pointer device, or a relative pointer device.
@@ -196,6 +207,182 @@ angular.module('settings').provider('preferenceService', ['$injector',
          */
         service.preferences = provider.preferences;
 
+        // Downstream modification: retain the automatic refresh loop guard in
+        // browser session storage so it survives the refresh that it controls.
+        var AUTO_REFRESH_STATE_KEY = 'GUAC_AUTO_REFRESH_STATE';
+        var AUTO_REFRESH_DELAY = 5000;
+        var AUTO_REFRESH_MAX_ATTEMPTS = 2;
+        var AUTO_REFRESH_RESET_DELAY = 60000;
+        var automaticRefreshFallbackState = null;
+        var automaticRefreshStorageAvailable = true;
+
+        /**
+         * Returns the browser location represented by the current refresh
+         * state. Attempts for another route must never affect this page.
+         */
+        var getAutomaticRefreshTarget = function getAutomaticRefreshTarget() {
+            return $window.location.pathname
+                + $window.location.search
+                + $window.location.hash;
+        };
+
+        /**
+         * Returns valid automatic refresh state for the current page.
+         */
+        var getAutomaticRefreshState = function getAutomaticRefreshState() {
+            var state = automaticRefreshFallbackState;
+
+            try {
+                var stored = $window.sessionStorage.getItem(AUTO_REFRESH_STATE_KEY);
+                state = stored ? JSON.parse(stored) : null;
+            }
+            catch (e) {
+                // Disable automatic refresh if its cross-reload loop guard
+                // cannot be guaranteed. Manual refresh remains available.
+                automaticRefreshStorageAvailable = false;
+            }
+
+            var now = new Date().getTime();
+            if (!state || state.target !== getAutomaticRefreshTarget()
+                    || typeof state.attempts !== 'number'
+                    || state.attempts < 0
+                    || Math.floor(state.attempts) !== state.attempts
+                    || typeof state.lastAttempt !== 'number'
+                    || state.lastAttempt < 0
+                    || now - state.lastAttempt >= AUTO_REFRESH_RESET_DELAY) {
+                state = {
+                    target : getAutomaticRefreshTarget(),
+                    attempts : 0,
+                    lastAttempt : 0
+                };
+            }
+
+            return state;
+        };
+
+        /**
+         * Persists automatic refresh state for the current browser tab.
+         */
+        var setAutomaticRefreshState = function setAutomaticRefreshState(state) {
+            automaticRefreshFallbackState = state;
+            try {
+                $window.sessionStorage.setItem(
+                    AUTO_REFRESH_STATE_KEY, JSON.stringify(state));
+                return true;
+            }
+            catch (e) {
+                automaticRefreshStorageAvailable = false;
+                return false;
+            }
+        };
+
+        /**
+         * Returns whether another automatic page refresh is allowed.
+         */
+        service.canAutomaticallyRefresh = function canAutomaticallyRefresh() {
+            var state = getAutomaticRefreshState();
+            return automaticRefreshStorageAvailable
+                && state.attempts < AUTO_REFRESH_MAX_ATTEMPTS;
+        };
+
+        /**
+         * Returns the bounded delay before the next automatic page refresh.
+         */
+        service.getAutomaticRefreshDelay = function getAutomaticRefreshDelay() {
+            return AUTO_REFRESH_DELAY * Math.pow(2,
+                getAutomaticRefreshState().attempts);
+        };
+
+        /**
+         * Clears the automatic page refresh loop guard after stable operation.
+         */
+        service.resetAutomaticRefresh = function resetAutomaticRefresh() {
+            automaticRefreshFallbackState = null;
+            try {
+                $window.sessionStorage.removeItem(AUTO_REFRESH_STATE_KEY);
+            }
+            catch (e) {
+                // The in-memory fallback has already been cleared.
+            }
+        };
+
+        /**
+         * Revalidates the current document before reloading, avoiding a mixed
+         * frontend when an image upgrade or rollback leaves stale HTML or
+         * JavaScript in an intermediary/browser cache.
+         */
+        var reloadCurrentPage = function reloadCurrentPage() {
+            var reloaded = false;
+            var reloadFallback = null;
+
+            var reload = function reload() {
+                if (reloaded)
+                    return;
+                reloaded = true;
+                if (reloadFallback !== null)
+                    $window.clearTimeout(reloadFallback);
+                $window.location.reload(true);
+            };
+
+            // Allow Guacamole's built-in build identifier check to perform a
+            // fresh cache recovery attempt after upgrades and rollbacks.
+            try {
+                $window.sessionStorage.removeItem('reloadedFor');
+            }
+            catch (e) {
+                // Cache revalidation below remains available.
+            }
+
+            try {
+                var request = new $window.XMLHttpRequest();
+                request.open('GET', $window.location.href, true);
+                request.setRequestHeader('Cache-Control', 'no-cache');
+                request.setRequestHeader('Pragma', 'no-cache');
+                request.timeout = 3000;
+                request.onreadystatechange = function pageRevalidated() {
+                    if (request.readyState === $window.XMLHttpRequest.DONE)
+                        reload();
+                };
+                request.onerror = reload;
+                request.ontimeout = reload;
+                request.send();
+                reloadFallback = $window.setTimeout(reload, 5000);
+            }
+            catch (e) {
+                reload();
+            }
+        };
+
+        /**
+         * Refreshes the current browser page, recording automatic attempts or
+         * clearing the guard for an explicit manual refresh.
+         *
+         * @param {Boolean} manual
+         *     Whether the user explicitly requested this refresh.
+         *
+         * @returns {Boolean}
+         *     true if the page refresh was requested, false if the automatic
+         *     refresh limit has already been reached.
+         */
+        service.refreshPage = function refreshPage(manual) {
+            var state = getAutomaticRefreshState();
+
+            if (manual)
+                service.resetAutomaticRefresh();
+            else {
+                if (!automaticRefreshStorageAvailable
+                        || state.attempts >= AUTO_REFRESH_MAX_ATTEMPTS)
+                    return false;
+                state.attempts++;
+                state.lastAttempt = new Date().getTime();
+                if (!setAutomaticRefreshState(state))
+                    return false;
+            }
+
+            reloadCurrentPage();
+            return true;
+        };
+
         /**
          * Persists the current values of all preferences, if possible.
          */
diff --git a/guacamole/src/main/frontend/src/app/settings/templates/settingsPreferences.html b/guacamole/src/main/frontend/src/app/settings/templates/settingsPreferences.html
--- a/guacamole/src/main/frontend/src/app/settings/templates/settingsPreferences.html
+++ b/guacamole/src/main/frontend/src/app/settings/templates/settingsPreferences.html
@@ -24,6 +24,20 @@
         </div>
     </div>
 
+    <!-- Connection recovery -->
+    <h2 class="header">{{'SETTINGS_PREFERENCES.SECTION_HEADER_CONNECTION_RECOVERY' | translate}}</h2>
+    <div class="settings section connection-recovery">
+        <p>{{'SETTINGS_PREFERENCES.HELP_CONNECTION_RECOVERY' | translate}}</p>
+        <div class="form">
+            <table class="fields">
+                <tr>
+                    <th>{{'SETTINGS_PREFERENCES.FIELD_HEADER_AUTOMATIC_REFRESH' | translate}}</th>
+                    <td><input ng-model="preferences.automaticRefresh" type="checkbox"/></td>
+                </tr>
+            </table>
+        </div>
+    </div>
+
     <!-- Password update -->
     <h2 class="header" ng-show="canUpdateSelf">{{'SETTINGS_PREFERENCES.SECTION_HEADER_UPDATE_PASSWORD' | translate}}</h2>
     <div class="settings section update-password" ng-show="canUpdateSelf">
diff --git a/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js b/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js
--- a/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js
+++ b/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js
@@ -28,8 +28,10 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
         scope: {},
 
         templateUrl: 'app/textInput/templates/guacTextInput.html',
-        controller: ['$scope', '$rootScope', '$element', '$timeout',
-            function guacTextInput($scope, $rootScope, $element, $timeout) {
+        controller: ['$scope', '$rootScope', '$element', '$timeout', '$window',
+            function guacTextInput($scope, $rootScope, $element, $timeout, $window) {
+
+            // Downstream modification: recover IME input after browser tab changes.
 
             /**
              * The number of characters to include on either side of text input
@@ -118,6 +120,13 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
              */
             var target = $element.find('.target')[0];
 
+            /**
+             * The browser document containing the text input target.
+             *
+             * @type Document
+             */
+            var document = $window.document;
+
             /**
              * Whether the text input target currently has focus. Setting this
              * attribute has no effect, but any bound property will be updated
@@ -129,7 +138,9 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
 
             target.onfocus = function targetFocusGained() {
                 hasFocus = true;
+                composingText = false;
                 resetTextInputTarget(TEXT_INPUT_PADDING);
+                $rootScope.$broadcast('guacTextInputFocusRequested');
             };
 
             target.onblur = function targetFocusLost() {
@@ -149,7 +160,20 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
             }, false);
 
             target.addEventListener("compositionend", function targetComposeEnd(e) {
+
                 composingText = false;
+
+                // Some browsers deliver the final input event before the
+                // compositionend event. In that case the input event is ignored
+                // while composingText is true. Process any remaining committed
+                // text once composition has ended.
+                $window.setTimeout(function flushCompletedComposition() {
+                    var paddingChar = String.fromCharCode(TEXT_INPUT_PADDING_CODEPOINT);
+                    var emptyValue = new Array(TEXT_INPUT_PADDING * 2 + 1).join(paddingChar);
+                    if (!composingText && target.value !== emptyValue)
+                        processTextInput();
+                }, 0);
+
             }, false);
 
             /**
@@ -281,7 +305,14 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
 
             };
 
-            target.addEventListener("input", function(e) {
+            /**
+             * Processes the current contents of the text input target, sending
+             * the corresponding text, Backspace, or Delete events remotely.
+             *
+             * @param {InputEvent} [e]
+             *     The browser input event that triggered processing, if any.
+             */
+            var processTextInput = function processTextInput(e) {
 
                 // Ignore input events during text composition
                 if (composingText)
@@ -311,9 +342,120 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
 
                 // Reset content
                 resetTextInputTarget(TEXT_INPUT_PADDING);
-                e.preventDefault();
+                if (e)
+                    e.preventDefault();
+
+            };
+
+            target.addEventListener("input", processTextInput, false);
+
+            /**
+             * Clears composition state after the browser page loses or regains
+             * visibility/focus. A browser may omit compositionend while a tab
+             * is backgrounded, otherwise leaving composingText permanently true.
+             */
+            var resetAfterBrowserFocusChange = function resetAfterBrowserFocusChange() {
+
+                composingText = false;
+                resetTextInputTarget(TEXT_INPUT_PADDING);
+
+                // Ensure synthetic text, Backspace, and Delete events are routed
+                // to an active Guacamole client after returning to this tab.
+                if (!document.hidden)
+                    $rootScope.$broadcast('guacTextInputFocusRequested');
+
+            };
+
+            document.addEventListener("visibilitychange", resetAfterBrowserFocusChange, false);
+            $window.addEventListener("focus", resetAfterBrowserFocusChange, false);
+
+            /**
+             * Returns whether focus intentionally belongs to a visible local
+             * form control instead of the remote text-input target.
+             *
+             * @param {Element} element
+             *     The element to test.
+             *
+             * @returns {boolean}
+             */
+            var isVisibleLocalInput = function isVisibleLocalInput(element) {
+
+                while (element && element !== document.body) {
+
+                    if (element === target)
+                        return false;
+
+                    var tagName = element.tagName
+                            ? element.tagName.toLowerCase() : '';
+                    var isLocalInput = tagName === 'input'
+                            || tagName === 'textarea'
+                            || tagName === 'select'
+                            || tagName === 'button'
+                            || tagName === 'a'
+                            || element.isContentEditable;
+
+                    if (isLocalInput) {
+                        var rect = element.getBoundingClientRect();
+                        return rect.width > 0 && rect.height > 0
+                                && rect.right > 0 && rect.bottom > 0;
+                    }
+
+                    element = element.parentNode;
+
+                }
+
+                return false;
+
+            };
+
+            /**
+             * Restores focus to the visible text-input target without stealing
+             * focus from a local Guacamole form control.
+             */
+            var focusTextInputTarget = function focusTextInputTarget() {
+
+                if (document.hidden)
+                    return;
+
+                var focused = document.activeElement;
+                if (!isVisibleLocalInput(focused)) {
+
+                    // Recreate the native editing context if Chromium kept this
+                    // textarea active while suspending its IME session.
+                    if (focused === target)
+                        target.blur();
+                    target.focus();
+
+                }
+
+            };
+
+            // If global keyboard restoration occurs while text-input mode is
+            // active, keep focus on this visible textarea instead of allowing
+            // the hidden raw-keyboard InputSink to steal it.
+            $scope.$on('guacInputFocusRestoreRequested', function restoreTextInputFocus(event, immediate) {
+
+                event.preventDefault();
+                composingText = false;
+                resetTextInputTarget(TEXT_INPUT_PADDING);
+                $rootScope.$broadcast('guacTextInputFocusRequested');
+
+                // A pointerdown-triggered restoration must remain synchronous so
+                // Chromium associates the new editing context with that trusted
+                // user gesture. Other restoration events remain deferred.
+                if (immediate)
+                    focusTextInputTarget();
+                else
+                    $window.setTimeout(focusTextInputTarget, 0);
+
+            });
+
+            // Prevent stale handlers after navigating away from the client view.
+            $scope.$on('$destroy', function textInputDestroyed() {
+                document.removeEventListener("visibilitychange", resetAfterBrowserFocusChange, false);
+                $window.removeEventListener("focus", resetAfterBrowserFocusChange, false);
+            });
 
-            }, false);
 
             // Do not allow event target contents to be selected during input
             target.addEventListener("selectstart", function(e) {
diff --git a/guacamole/src/main/frontend/src/translations/en.json b/guacamole/src/main/frontend/src/translations/en.json
--- a/guacamole/src/main/frontend/src/translations/en.json
+++ b/guacamole/src/main/frontend/src/translations/en.json
@@ -74,6 +74,8 @@
         "ACTION_NAVIGATE_BACK"             : "@:APP.ACTION_NAVIGATE_BACK",
         "ACTION_NAVIGATE_HOME"             : "@:APP.ACTION_NAVIGATE_HOME",
         "ACTION_RECONNECT"                 : "Reconnect",
+        "ACTION_DISMISS_RECOVERY"          : "Do not refresh",
+        "ACTION_REFRESH_PAGE"              : "Refresh page",
         "ACTION_SAVE_FILE"                 : "@:APP.ACTION_SAVE",
         "ACTION_SHARE"                     : "@:APP.ACTION_SHARE",
         "ACTION_SHOW_CLIPBOARD"            : "Click to view clipboard contents.",
@@ -124,10 +126,15 @@
 
         "FIELD_PLACEHOLDER_FILTER" : "@:APP.FIELD_PLACEHOLDER_FILTER",
 
+        "ACTION_RECOVER_KEYBOARD" : "Recapture keyboard",
+
         "HELP_CLIPBOARD"           : "Text copied/cut within Guacamole will appear here. Changes to the text below will affect the remote clipboard.",
         "HELP_INPUT_METHOD_NONE"   : "No input method is used. Keyboard input is accepted from a connected, physical keyboard.",
         "HELP_INPUT_METHOD_OSK"    : "Display and accept input from the built-in Guacamole on-screen keyboard. The on-screen keyboard allows typing of key combinations that may otherwise be impossible (such as Ctrl-Alt-Del).",
         "HELP_INPUT_METHOD_TEXT"   : "Allow typing of text, and emulate keyboard events based on the typed text. This is necessary for devices such as mobile phones that lack a physical keyboard.",
+        "HELP_RECOVER_KEYBOARD"    : "Reset and recapture keyboard and IME input without refreshing. You can also press Ctrl-Alt-K.",
+        "HELP_REFRESH_PAGE_NOW"    : "Reload the complete Guacamole page after revalidating cached frontend files. Use this when the remote display remains black or never finishes opening.",
+        "HELP_AUTOMATIC_REFRESH"   : "After a confirmed network or control failure, wait briefly and refresh this entire page automatically. If disabled, use Refresh page in the lower-right warning. A full refresh may open a different backend or remote session.",
         "HELP_MOUSE_MODE"          : "Determines how the remote mouse behaves with respect to touches.",
         "HELP_MOUSE_MODE_ABSOLUTE" : "Tap to click. The click occurs at the location of the touch.",
         "HELP_MOUSE_MODE_RELATIVE" : "Drag to move the mouse pointer and tap to click. The click occurs at the location of the pointer.",
@@ -147,9 +154,11 @@
         "NAME_KEY_TAB"             : "Tab",
         "NAME_MOUSE_MODE_ABSOLUTE" : "Touchscreen",
         "NAME_MOUSE_MODE_RELATIVE" : "Touchpad",
+        "NAME_AUTOMATIC_REFRESH"   : "Delayed automatic page refresh",
 
         "SECTION_HEADER_CLIENT_MESSAGES" : "Messages",
         "SECTION_HEADER_CLIPBOARD"       : "Clipboard",
+        "SECTION_HEADER_CONNECTION_RECOVERY" : "Connection recovery",
         "SECTION_HEADER_DEVICES"         : "Devices",
         "SECTION_HEADER_DISPLAY"         : "Display",
         "SECTION_HEADER_FILE_TRANSFERS"  : "File Transfers",
@@ -164,10 +173,13 @@
         "TEXT_CLIENT_STATUS_CONNECTING"   : "Connecting to Guacamole...",
         "TEXT_CLIENT_STATUS_DISCONNECTED" : "You have been disconnected.",
         "TEXT_CLIENT_STATUS_UNSTABLE"     : "The network connection to the Guacamole server appears unstable.",
+        "TEXT_CLIENT_STATUS_RECOVERED_SLOW" : "The display or controls may be unresponsive. Refresh this page to recover.",
+        "TEXT_CLIENT_STATUS_RECOVERED_REFRESHING" : "This page will refresh automatically after a short delay.",
         "TEXT_CLIENT_STATUS_WAITING"      : "Connected to Guacamole. Waiting for response...",
         "TEXT_USER_JOINED"                : "{USERNAME} has joined the connection.",
         "TEXT_USER_LEFT"                  : "{USERNAME} has left the connection.",
         "TEXT_RECONNECT_COUNTDOWN"        : "Reconnecting in {REMAINING} {REMAINING, plural, one{second} other{seconds}}...",
+        "TEXT_REFRESH_PAGE_COUNTDOWN"     : "Refreshing this page in {REMAINING} {REMAINING, plural, one{second} other{seconds}}...",
         "TEXT_FILE_TRANSFER_PROGRESS"     : "{PROGRESS} {UNIT, select, b{B} kb{KB} mb{MB} gb{GB} other{}}",
 
         "URL_OSK_LAYOUT" : "layouts/en-us-qwerty.json"
@@ -1077,6 +1089,7 @@
         "ERROR_PASSWORD_MISMATCH" : "@:APP.ERROR_PASSWORD_MISMATCH",
 
         "FIELD_HEADER_LANGUAGE"                  : "Display language:",
+        "FIELD_HEADER_AUTOMATIC_REFRESH"         : "Delayed automatic page refresh:",
         "FIELD_HEADER_NUMBER_RECENT_CONNECTIONS" : "Number of Recent Connections to show:",
         "FIELD_HEADER_PASSWORD"                  : "Password:",
         "FIELD_HEADER_PASSWORD_OLD"              : "Current Password:",
@@ -1087,6 +1100,7 @@
         "FIELD_HEADER_USERNAME"                  : "Username:",
         
         "HELP_APPEARANCE"           : "Here you can enable or disable whether the \"Recent Connections\" section is shown on the home screen, and adjust the number of recent connections included.",
+        "HELP_CONNECTION_RECOVERY"  : "When enabled, confirmed network or control failures refresh the entire current page after a short delay. When disabled, use the lower-right manual action. Full refresh may select another backend or remote session.",
         "HELP_DEFAULT_INPUT_METHOD" : "The default input method determines how keyboard events are received by Guacamole. Changing this setting may be necessary when using a mobile device, or when typing through an IME. This setting can be overridden on a per-connection basis within the Guacamole menu.",
         "HELP_DEFAULT_MOUSE_MODE"   : "The default mouse emulation mode determines how the remote mouse will behave in new connections with respect to touches. This setting can be overridden on a per-connection basis within the Guacamole menu.",
         "HELP_INPUT_METHOD_NONE"    : "@:CLIENT.HELP_INPUT_METHOD_NONE",
@@ -1105,6 +1119,7 @@
         "NAME_INPUT_METHOD_TEXT" : "@:CLIENT.NAME_INPUT_METHOD_TEXT",
 
         "SECTION_HEADER_APPEARANCE"           : "Appearance",
+        "SECTION_HEADER_CONNECTION_RECOVERY"  : "Connection Recovery",
         "SECTION_HEADER_DEFAULT_INPUT_METHOD" : "Default Input Method",
         "SECTION_HEADER_DEFAULT_MOUSE_MODE"   : "Default Mouse Emulation Mode",
         "SECTION_HEADER_UPDATE_PASSWORD"      : "Change Password"
diff --git a/guacamole/src/main/frontend/src/translations/zh.json b/guacamole/src/main/frontend/src/translations/zh.json
--- a/guacamole/src/main/frontend/src/translations/zh.json
+++ b/guacamole/src/main/frontend/src/translations/zh.json
@@ -70,6 +70,8 @@
         "ACTION_NAVIGATE_BACK"             : "@:APP.ACTION_NAVIGATE_BACK",
         "ACTION_NAVIGATE_HOME"             : "@:APP.ACTION_NAVIGATE_HOME",
         "ACTION_RECONNECT"                 : "重新连接",
+        "ACTION_DISMISS_RECOVERY"          : "暂不刷新",
+        "ACTION_REFRESH_PAGE"              : "刷新页面",
         "ACTION_SAVE_FILE"                 : "@:APP.ACTION_SAVE",
         "ACTION_SHARE"                     : "@:APP.ACTION_SHARE",
         "ACTION_SHOW_CLIPBOARD"            : "点击以查看剪贴板内容。",
@@ -120,10 +122,15 @@
 
         "FIELD_PLACEHOLDER_FILTER" : "@:APP.FIELD_PLACEHOLDER_FILTER",
 
+        "ACTION_RECOVER_KEYBOARD" : "重新捕获键盘",
+
         "HELP_CLIPBOARD"           : "复制/剪切的文本将出现在这里。对下面文本内容所作的修改将会影响远程电脑上的剪贴板。",
         "HELP_INPUT_METHOD_NONE"   : "没有选择任何输入方式。将从连接的物理键盘接受键盘输入。",
         "HELP_INPUT_METHOD_OSK"    : "显示并从内建的 Guacamole 屏幕键盘接受输入。屏幕键盘可以输入平常无法输入的按键组合（如 Ctrl-Alt-Del 等）。",
         "HELP_INPUT_METHOD_TEXT"   : "允许输入文本，并根据所输入的文本模拟键盘事件。可用于没有物理键盘的设备，如手机等。",
+        "HELP_RECOVER_KEYBOARD"    : "无需刷新即可重置并重新捕获键盘和输入法。也可按 Ctrl-Alt-K。",
+        "HELP_REFRESH_PAGE_NOW"    : "重新验证已缓存的前端文件后，完整刷新 Guacamole 页面。远程画面持续黑屏或一直无法打开时可使用。",
+        "HELP_AUTOMATIC_REFRESH"   : "确认网络或控制失效后，短暂等待并自动刷新整个页面。关闭时请使用右下角的“刷新页面”。整页刷新可能打开不同的后端或远程会话。",
         "HELP_MOUSE_MODE"          : "设置远程电脑上的鼠标对触控行为的反应。",
         "HELP_MOUSE_MODE_ABSOLUTE" : "点击时立即触发按键。在点击的位置触发鼠标按键事件。",
         "HELP_MOUSE_MODE_RELATIVE" : "拖拽时移动鼠标，再点击时触发按键。在鼠标当前所在的位置触发按键事件。",
@@ -143,9 +150,11 @@
         "NAME_KEY_TAB"             : "Tab",
         "NAME_MOUSE_MODE_ABSOLUTE" : "触摸屏",
         "NAME_MOUSE_MODE_RELATIVE" : "触控板",
+        "NAME_AUTOMATIC_REFRESH"   : "延迟自动刷新整个页面",
 
         "SECTION_HEADER_CLIENT_MESSAGES" : "消息",
         "SECTION_HEADER_CLIPBOARD"       : "剪贴板",
+        "SECTION_HEADER_CONNECTION_RECOVERY" : "连接恢复",
         "SECTION_HEADER_DEVICES"         : "设备",
         "SECTION_HEADER_DISPLAY"         : "显示",
         "SECTION_HEADER_FILE_TRANSFERS"  : "文件传输",
@@ -160,10 +169,13 @@
         "TEXT_CLIENT_STATUS_CONNECTING"   : "正在连接 Guacamole...",
         "TEXT_CLIENT_STATUS_DISCONNECTED" : "您的连接已断开。",
         "TEXT_CLIENT_STATUS_UNSTABLE"     : "到 Guacamole 服务器的网络连接似乎不太稳定。",
+        "TEXT_CLIENT_STATUS_RECOVERED_SLOW" : "画面或控制可能仍然无响应，请刷新当前页面。",
+        "TEXT_CLIENT_STATUS_RECOVERED_REFRESHING" : "页面将在短暂延迟后自动刷新。",
         "TEXT_CLIENT_STATUS_WAITING"      : "已连接到 Guacamole。正在等待应答...",
         "TEXT_USER_JOINED"                : "用户 {USERNAME} 已加入连接。",
         "TEXT_USER_LEFT"                  : "用户 {USERNAME} 断开了连接。",
         "TEXT_RECONNECT_COUNTDOWN"        : "在 {REMAINING} 秒后重连...",
+        "TEXT_REFRESH_PAGE_COUNTDOWN"     : "将在 {REMAINING} 秒后刷新整个页面...",
         "TEXT_FILE_TRANSFER_PROGRESS"     : "{PROGRESS} {UNIT, select, b{B} kb{KB} mb{MB} gb{GB} other{}}",
 
         "URL_OSK_LAYOUT" : "layouts/en-us-qwerty.json"
@@ -1011,6 +1023,7 @@
         "ERROR_PASSWORD_MISMATCH" : "@:APP.ERROR_PASSWORD_MISMATCH",
 
         "FIELD_HEADER_LANGUAGE"           : "界面语言：",
+        "FIELD_HEADER_AUTOMATIC_REFRESH"  : "延迟自动刷新整个页面：",
         "FIELD_HEADER_PASSWORD"           : "密码：",
         "FIELD_HEADER_PASSWORD_OLD"       : "当前密码：",
         "FIELD_HEADER_PASSWORD_NEW"       : "新密码：",
@@ -1018,6 +1031,7 @@
         "FIELD_HEADER_TIMEZONE"           : "时区：",
         "FIELD_HEADER_USERNAME"           : "用户名：",
         
+        "HELP_CONNECTION_RECOVERY"  : "开启后，确认网络或控制失效时会延迟刷新整个当前页面；关闭后请使用右下角的手动按钮。整页刷新可能重新选择后端或远程会话。",
         "HELP_DEFAULT_INPUT_METHOD" : "默认输入法决定了 Guacamole 如何接收键盘事件。当使用移动设备或使用 IME 输入时，有可能需要更改设置。本设置可在 Guacamole 菜单内被单个连接的设定覆盖。",
         "HELP_DEFAULT_MOUSE_MODE"   : "默认鼠标模拟方式决定了新连接内的远程鼠标如何响应屏幕触控。本设置可在 Guacamole 菜单内被单个连接的设定覆盖。",
         "HELP_INPUT_METHOD_NONE"    : "@:CLIENT.HELP_INPUT_METHOD_NONE",
@@ -1036,6 +1050,7 @@
         "NAME_INPUT_METHOD_TEXT" : "@:CLIENT.NAME_INPUT_METHOD_TEXT",
 
         "SECTION_HEADER_DEFAULT_INPUT_METHOD" : "缺省输入法",
+        "SECTION_HEADER_CONNECTION_RECOVERY"  : "连接恢复",
         "SECTION_HEADER_DEFAULT_MOUSE_MODE"   : "缺省鼠标模拟方式",
         "SECTION_HEADER_UPDATE_PASSWORD"      : "更改密码"
 
